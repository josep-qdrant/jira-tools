- <🟢/🟡/🔴> **[[<milestone-slug>|<KEY>]]** <Milestone title> — <status> · size <T-Shirt Size or "not set"> · <n> children (<status breakdown, e.g. "2 Done, 1 In Progress">) · window <start date>→<target date>
  **Sprint fit:** <the single fact that gates the verdict, one line>. <Only if not 🟢: "Needs: <one concrete next step — pick from: not decomposed, size not set, missing definitions/AC, missing UI/design, not extrapolable, missing context, unrealistic estimate>.">

<!--
One block per Milestone. No seven-row table, no Deductions-to-verify block, no
paragraph-length rationale — this is the condensed rendering (see
definition-of-ready SKILL.md, "Applying DoR to a Milestone"). If a Milestone's
call is genuinely contested and needs the full rubric shown, give it its own
standalone card with the full DoR block instead of expanding this line.
-->
